# Настройка Email для контактной формы

## 📧 Что реализовано:

✅ Контактная форма на главной странице с полями: имя, email, тема (опционально), сообщение
✅ Сохранение всех сообщений в базу данных PostgreSQL
✅ Отправка уведомлений на email администратора
✅ Страница в админке для просмотра сообщений (`/dashboard/messages`)
✅ Функции: пометить как прочитанное, удалить, ответить через email

## 🔧 Настройка Email (Gmail)

Для работы отправки email нужно настроить переменные окружения:

### 1. Создайте App Password для Gmail

1. Перейдите в настройки аккаунта Google: https://myaccount.google.com/security
2. Включите двухфакторную аутентификацию (если ещё не включена)
3. Перейдите в "App passwords": https://myaccount.google.com/apppasswords
4. Создайте новый App Password:
   - Выберите приложение: "Mail"
   - Выберите устройство: "Other (Custom name)" → введите "Portfolio Website"
5. Скопируйте сгенерированный пароль (16 символов)

### 2. Добавьте переменные в `.env`

Откройте файл `.env` в корне проекта и добавьте:

```env
# Email Configuration
EMAIL_USER=kazimi.dev@gmail.com
EMAIL_PASSWORD=your_16_char_app_password_here
ADMIN_EMAIL=kazimi.dev@gmail.com
```

**Важно:** 
- `EMAIL_PASSWORD` — это НЕ ваш обычный пароль от Gmail, а App Password
- Замените `your_16_char_app_password_here` на реальный App Password
- Не публикуйте `.env` в git (он уже в `.gitignore`)

### 3. Перезапустите dev сервер

После добавления переменных окружения:

```bash
npm run dev
```

## 📝 Как это работает:

1. **Пользователь отправляет сообщение** через форму на главной странице
2. **Сообщение сохраняется в БД** в таблицу `ContactMessage`
3. **Email уведомление отправляется** на `kazimi.dev@gmail.com`
4. **Админ видит сообщения** в разделе `/dashboard/messages`:
   - Список всех сообщений (непрочитанные выделены)
   - Просмотр деталей сообщения
   - Кнопка "Reply via Email" для быстрого ответа
   - Удаление сообщений

## 🗄️ База данных

Нужно запустить миграцию для создания таблицы `ContactMessage`:

```bash
npx prisma migrate dev --name add_contact_messages
```

Или если проект уже развёрнут:

```bash
npx prisma db push
```

## 🎨 Функции в админке:

- 📬 **Просмотр сообщений** — список всех обращений
- 🔵 **Непрочитанные** — выделены синей полосой слева
- ✅ **Пометить прочитанным** — автоматически при открытии
- 📧 **Ответить** — открывает почтовый клиент с готовым ответом
- 🗑️ **Удалить** — удаление сообщения

## 🔒 Безопасность:

- ✅ Email credentials хранятся в `.env` (не в коде)
- ✅ Просмотр сообщений только для авторизованных админов
- ✅ Валидация email адресов на клиенте и сервере
- ✅ Rate limiting можно добавить позже при необходимости

## 📧 Формат Email уведомления:

Красивое HTML письмо с:
- Градиентным заголовком
- Информацией об отправителе (имя, email)
- Темой сообщения (если указана)
- Текстом сообщения
- Кнопкой для прямого ответа

## 🧪 Тестирование:

1. Откройте главную страницу: http://localhost:3000
2. Прокрутите вниз до секции "Contact"
3. Заполните и отправьте форму
4. Проверьте email `kazimi.dev@gmail.com`
5. Зайдите в админку: http://localhost:3000/dashboard/messages
6. Просмотрите полученное сообщение

## ⚠️ Важно для продакшена:

При деплое на сервер (VPS, Vercel и т.д.):
1. Добавьте те же переменные окружения на сервере
2. Запустите миграцию БД: `npx prisma migrate deploy`
3. Убедитесь что порты 465/587 (SMTP) не заблокированы

## 🔄 Альтернативы Gmail:

Если нужно использовать другой email сервис, измените в `src/lib/email.ts`:

**Для Yandex:**
```typescript
service: 'Yandex',
```

**Для custom SMTP:**
```typescript
host: 'smtp.yourserver.com',
port: 587,
secure: false,
```

## 📱 Дополнительные улучшения (опционально):

- [ ] Push уведомления при новом сообщении
- [ ] Email шаблоны для ответов
- [ ] Статистика сообщений (по дням/месяцам)
- [ ] Фильтрация по прочитанным/непрочитанным
- [ ] Поиск по сообщениям
- [ ] Экспорт сообщений в CSV

---

Готово! Система полностью рабочая, нужно только настроить Gmail App Password 🚀
