# Kamran Kazimi Portfolio - Full Stack

Современный полнофункциональный портфолио-сайт с административной панелью для управления контентом.

## 🚀 Возможности

### Frontend
- ✅ Многоязычность (Азербайджанский, Русский, Английский)
- ✅ Темная/Светлая тема с сохранением предпочтений
- ✅ Анимации при скролле
- ✅ Адаптивный дизайн
- ✅ Современный UI с Tailwind CSS

### Backend
- ✅ **NextAuth.js** - Безопасная авторизация администратора
- ✅ **Prisma ORM** - Работа с базой данных
- ✅ **SQLite** - Легковесная база данных
- ✅ **API Routes** - RESTful API для всех операций
- ✅ **Защищенные роуты** - Middleware для проверки авторизации

### Админ Панель
- ✅ Редактор Hero секции (на 3 языках)
- ✅ Редактор About секции (на 3 языках)
- ✅ Управление контактной информацией
- ✅ Менеджер проектов (CRUD операции)
- ✅ Менеджер навыков (по категориям)
- ✅ Кнопка выхода из системы

## 📦 Технологический стек

- **Framework**: Next.js 15.5
- **React**: 19.1
- **TypeScript**: 5.x
- **Styling**: Tailwind CSS 4.x
- **Database**: SQLite + Prisma ORM
- **Authentication**: NextAuth.js
- **Security**: bcryptjs для хеширования паролей

## 🛠 Установка и запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка базы данных
```bash
# Создание миграций
npx prisma migrate dev --name init

# Заполнение начальными данными (создание админа)
npx tsx prisma/seed.ts
```

### 3. Запуск проекта
```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

## 🔐 Доступ к админ панели

### Данные для входа по умолчанию:
- **Email**: `admin@kamran.dev`
- **Пароль**: `admin123`

⚠️ **ВАЖНО**: Измените пароль после первого входа в продакшене!

### URL админ панели:
- Вход: `http://localhost:3000/login`
- Dashboard: `http://localhost:3000/dashboard`
- Редактор контента: `http://localhost:3000/dashboard/content`
- Менеджер проектов: `http://localhost:3000/dashboard/projects`

## 📁 Структура проекта

```
├── prisma/
│   ├── schema.prisma      # Схема базы данных
│   ├── seed.ts           # Скрипт заполнения БД
│   └── migrations/       # Миграции БД
├── src/
│   ├── app/
│   │   ├── api/          # API endpoints
│   │   │   ├── auth/     # NextAuth
│   │   │   ├── content/  # Hero, About
│   │   │   ├── projects/ # CRUD проектов
│   │   │   ├── skills/   # CRUD навыков
│   │   │   └── contact/  # Контакты
│   │   ├── dashboard/    # Админ панель
│   │   │   ├── content/  # Редактор контента
│   │   │   └── projects/ # Менеджер проектов
│   │   ├── login/        # Страница входа
│   │   └── page.tsx      # Главная страница
│   ├── components/       # React компоненты
│   ├── lib/             # Утилиты и хуки
│   ├── types/           # TypeScript типы
│   └── middleware.ts    # Защита роутов
└── .env                 # Переменные окружения
```

## 🗄 База данных

### Модели:
- **Admin** - Администраторы системы
- **HeroContent** - Контент главной секции
- **AboutContent** - Контент раздела "Обо мне"
- **Project** - Портфолио проекты
- **Skill** - Технические навыки
- **ContactInfo** - Контактная информация

### Prisma команды:
```bash
# Открыть Prisma Studio (GUI для БД)
npx prisma studio

# Создать новую миграцию
npx prisma migrate dev --name migration_name

# Применить миграции в продакшене
npx prisma migrate deploy

# Сгенерировать Prisma Client
npx prisma generate
```

## 🔧 API Endpoints

### Публичные (без авторизации):
- `GET /api/content/hero` - Получить Hero контент
- `GET /api/content/about` - Получить About контент
- `GET /api/projects` - Получить список проектов
- `GET /api/skills` - Получить список навыков
- `GET /api/contact` - Получить контактную информацию

### Защищенные (требуют авторизации):
- `PUT /api/content/hero` - Обновить Hero контент
- `PUT /api/content/about` - Обновить About контент
- `POST /api/projects` - Создать проект
- `PUT /api/projects/[id]` - Обновить проект
- `DELETE /api/projects/[id]` - Удалить проект
- `POST /api/skills` - Добавить навык
- `DELETE /api/skills/[id]` - Удалить навык
- `PUT /api/contact` - Обновить контакты

## 🚀 Деплой

### Подготовка к продакшену:

1. **Измените NEXTAUTH_SECRET в .env**:
```bash
# Сгенерируйте новый секрет
openssl rand -base64 32
```

2. **Обновите NEXTAUTH_URL**:
```env
NEXTAUTH_URL=https://ваш-домен.com
```

3. **Для продакшена лучше использовать PostgreSQL вместо SQLite**

### Vercel:
```bash
# Установка Vercel CLI
npm i -g vercel

# Деплой
vercel
```

### Docker:
```bash
# Собрать образ
docker build -t portfolio .

# Запустить контейнер
docker run -p 3000:3000 portfolio
```

## 📝 Как редактировать контент

1. Перейдите на `/login` и войдите в систему
2. В Dashboard выберите нужный раздел:
   - **Content Editor** - для редактирования Hero, About, Contact
   - **Projects & Skills** - для управления проектами и навыками
3. Внесите изменения
4. Нажмите "Save" для сохранения
5. Изменения сразу отобразятся на главной странице

## 🔒 Безопасность

- ✅ Пароли хешируются с помощью bcryptjs
- ✅ JWT токены для сессий
- ✅ Middleware защищает админ роуты
- ✅ CSRF защита встроена в Next.js
- ✅ API эндпоинты требуют авторизации для изменений

## 🎨 Кастомизация

### Изменить цветовую схему:
Отредактируйте `src/app/globals.css`

### Добавить новые языки:
1. Обновите типы в `src/types/content.ts`
2. Добавьте поля в схему Prisma
3. Обновите компоненты админки
4. Запустите `npx prisma migrate dev`

## 🤝 Поддержка

Если возникли вопросы или проблемы:
1. Проверьте логи в терминале
2. Откройте Prisma Studio: `npx prisma studio`
3. Проверьте консоль браузера на ошибки

## 📄 Лицензия

MIT License - используйте свободно!

---

**Автор**: Kamran Kazimi
**Email**: admin@kamran.dev
**GitHub**: [@kamran134](https://github.com/kamran134)
